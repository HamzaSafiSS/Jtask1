Basic spring Boot Application that respond to GET HTTP request.
The Application has only single Request Get /hello returns
a JSON message.

Features:
Built Using Spring Boot.
Directly return JSON message to the client.
@RestCOntroller mark the class as an HTTP request controller.
